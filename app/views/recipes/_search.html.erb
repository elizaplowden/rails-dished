<!-- [...] -->
<%= simple_form_for "search", url: recipes_path, method: :get  do |f| %>
  <%= f.label "What's in your fridge?", class: 'search-bar'%>
  <%= f.input :query, collection: foods.pluck(:name), label: false, input_html: { class: 'select2' }, placeholder: "What's in your fridge?"  %>
  <%= f.submit "Search", class: "btn btn-primary" %>
<% end %>
