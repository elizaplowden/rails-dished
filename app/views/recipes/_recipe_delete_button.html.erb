<%= link_to recipe_path(recipe.id), method: :delete, class: "black-btn", data: { confirm: 'Are you sure you want to delete your recipe?' } do %>
  <% if recipe.user == current_user %>
    <p><i class="fas fa-trash-alt"></i></i> Delete recipe</p>
  <% end %>
<% end %>
