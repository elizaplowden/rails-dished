<%= simple_form_for [@recipe, @review], remote: true do |f| %>
  <%= f.input :description %>
  <%= f.input :rating %>
  <%= f.submit "Create a Review", class: 'btn btn-primary' %>
<% end %>

<%#-- Link to recipes index%>
<%= link_to 'Back', recipe_path(@recipe.id), class: "button" %>
